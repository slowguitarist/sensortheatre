[package]
name = "sensortheatre"
version = "0.1.0"
edition = "2024"

[lib]
name = "sensortheatre"
crate-type = ["staticlib", "rlib"]

[target.'cfg(target_os = "none")'.dependencies]
panic-halt = "1.0.0"